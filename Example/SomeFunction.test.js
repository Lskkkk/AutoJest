test('test plus', () => {
    expect(plus(a,b)).toEqual(result);
});

test('test minus', () => {
    expect(minus(a,b)).toEqual(result);
});

test('test multiply', () => {
    expect(multiply(a,b)).toEqual(result);
});

